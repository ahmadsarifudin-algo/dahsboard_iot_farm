# ============================================================
# IoT Dashboard Backend — Environment Configuration
# ============================================================
# Copy this file to .env and adjust for your environment:
#   cp .env.example .env
#
# Supported environments:
#   - LOCAL DEV: SQLite, no Redis/MQTT (default values below)
#   - DOCKER DEV: Use docker-compose.yml defaults
#   - STAGING/PRODUCTION: TimescaleDB + Redis + external MQTT broker
# ============================================================

# --- App ---
APP_NAME=IoT Data Center Dashboard
DEBUG=true
# Set to "development", "staging", or "production"
ENVIRONMENT=development

# --- Database ---
# Local dev (SQLite — no external DB needed):
DATABASE_URL=sqlite+aiosqlite:///./iot_dashboard.db

# Docker dev (TimescaleDB):
# DATABASE_URL=postgresql+asyncpg://iot_user:iot_password@localhost:5432/iot_dashboard

# Production (TimescaleDB with SSL):
# DATABASE_URL=postgresql+asyncpg://user:password@db-host:5432/iot_dashboard?ssl=require

# --- Redis ---
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379
# Production:
# REDIS_ENABLED=true
# REDIS_URL=redis://:password@redis-host:6379/0

# --- MQTT (External Broker) ---
MQTT_ENABLED=false
MQTT_BROKER=broker.chickinindonesia.com
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=iot-backend
# Example when enabling external MQTT:
# MQTT_ENABLED=true
# MQTT_BROKER=broker.chickinindonesia.com
# MQTT_USERNAME=your-username
# MQTT_PASSWORD=your-password

# --- JWT Authentication ---
JWT_SECRET_KEY=dev-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# --- CORS ---
# Comma-separated origins or JSON array
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
# Production:
# CORS_ORIGINS=["https://your-domain.com"]

# --- Chickin Integration (external upstream services) ---
CHICKIN_AUTH_BASE_URL=https://auth.chickinindonesia.com
CHICKIN_IOT_BASE_URL=https://prod-iot.chickinindonesia.com
CHICKIN_MQTT_BROKER=broker.chickinindonesia.com
CHICKIN_MQTT_WS_PORT=8083

# --- Gemini AI (optional — can also be set via Settings page) ---
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-2.0-flash
